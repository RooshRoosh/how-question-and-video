encoding "utf8";

TTextMinerConfig {
    Dictionary = "dic.gzt";

    Input = {File="input.txt"}  // Вывод нужно делать или в файл или в stdin

    Output = {
        File="output.xml"
        Format=xml
    }
    Articles = [
        {Name = "первая_грамматика"}
    ]
    PrettyOutput = "pretty.html"

    Facts = [
        {Name="Q"}
    ]
}