encoding "utf8";

TTextMinerConfig {
    Dictionary = "dic.gzt";

    Input = {File="question.txt"}  // Вывод нужно делать или в файл или в stdin

    Output = {
        File="output.txt"
        Format=text
    }
    Articles = [
        {Name = "первая_грамматика"}
    ]
    PrettyOutput = "pretty.html"

    Facts = [
        {Name="Q"}
    ]
}